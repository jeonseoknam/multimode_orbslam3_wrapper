vocabulary/ORBvoc.txt
config/isaac
# NOTE: upstream ignored launch/ ; this pipeline installs it from CMakeLists
# and multimode_bringup/sensors.launch.py includes monocular.launch.py,
# so it must be tracked.
